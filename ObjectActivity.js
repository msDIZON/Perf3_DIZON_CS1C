let car = { 
    type: "Small", 
    model: "Ferrari", 
    color: "White" 
    
}; 

// Check the type of the car, then console
console.log(typeof car); 

// Change the property of type to "Toyota", then console
car.type = "Toyota"; 
console.log("Updated Type Property:", car); 
// Add a property which is wheel and has a value of 4, then console
car.wheels = 4; 
console.log("Added Property:", car);
